// Placeholder — regenerate after migrations are applied to a real project:
//   npx supabase gen types typescript --project-id <ref> > src/lib/types/database.ts
export type Database = Record<string, never>;
