---
name: growth-marketing-phase
description: "KasirKu shifted from build-mode to growth/marketing-mode 2026-07-18 — CRO audit, homepage pricing section shipped, Meta Ads next"
metadata: 
  node_type: memory
  type: project
  originSessionId: f00c880e-89f5-4451-9a5a-e61d5482df30
---

**2026-07-18**: Melan explicitly shifted focus from building the product to driving traffic, conversion, and closing sales — asked me to act as a senior growth marketer/CRO/ads specialist for KasirKu going forward, not just an engineer. Expect more requests framed around ads, copywriting, funnels, and conversion rather than pure feature work.

**CRO audit findings (first pass, homepage + funnel)**:
1. **Fixed 2026-07-18** (`f9e6621`): homepage had zero visible pricing — visitors had to sign up before seeing any price, which kills conversion for cold ad traffic. Added a public `#harga` section to [page.tsx](../../../PROJECT1/KasirKu/src/app/page.tsx) showing all 3 full-bundle plans (reusing the `PLANS` data from `src/lib/billing/plans.ts`, same pattern already used on `/sistem-akuntansi`), highlighted the yearly plan's ~44% savings vs monthly, linked from nav/hero/footer/mobile-nav.
2. **Fixed 2026-07-18** (`46d98de`): the "Dipercaya pemilik F&B, retail, dan tempat wisata" line (in `src/components/auth-shell.tsx`, the shared left-panel on login/signup/forgot-password) was an unsubstantiated customer-trust claim with zero real customers to back it. When asked to "lanjut sosial proof/testimoni," flagged that fabricating names/quotes would be dishonest (and risks ad-platform bans for fake testimonials) — Melan chose to remove the false claim and replace it with something honest instead: "Coba gratis, tanpa kartu kredit — langsung bisa dipakai dalam hitungan menit." Real testimonials remain a good next step once there are actual paying customers to ask (the 2 organic signups "HAHA"/alissafania58@gmail.com and "Kopi Blirik"/fjr.us.one@gmail.com are candidates to reach out to, but that's Melan's outreach to send, not something to fabricate).
3. **Structural blocker for scaling ad spend**: Midtrans is still pending business approval, so billing is manual (`BILLING_MANUAL_MODE`, see [[billing-midtrans]]) — paid conversions from ads won't be self-serve until that's resolved. Flagged to Melan as a priority to chase before spending seriously on ads, since cold-traffic closing will bottleneck on manual invoicing otherwise.
4. **Good existing assets identified as ad-funnel candidates**: the free Kalkulator HPP (`/kalkulator-hpp`) is low-friction (no signup needed to get value) and a stronger candidate for a first Meta/Google ad landing page than the general homepage; the 3 blog articles ([[mini-erp-scope]] era content) are repurposable as native ad creative.

**Manual WA closing (2026-07-18, `406e04c`)**: since Midtrans approval is still pending, Melan chose to lean fully into manual closing via WhatsApp for now. Discovered the app already had a full manual-billing flow (`BILLING_MANUAL_MODE = true` in `src/lib/billing/config.ts`, WA number `6281234556757`, email `create2impact.id@gmail.com`) wired into the in-dashboard billing page and the Kalkulator HPP Desktop purchase page — but the *public* pricing sections on `/` and `/sistem-akuntansi` (added earlier this session) only linked "Pilih Paket Ini" to `/signup`, forcing cold ad traffic through account creation before reaching a human. Added a secondary "💬 Tanya dulu via WhatsApp" link under each plan card on both public pages, pre-filled with the plan name + price via `wa.me`, reusing `BILLING_CONTACT`. Now a hot lead from ads can go straight to WA without signing up first. Flip `BILLING_MANUAL_MODE` back to `false` once Midtrans is approved and tested — that alone re-enables the automated `PayButton` flow on the dashboard billing page (the public pricing sections' `/signup` link and WA link can both stay, WA is still useful even post-Midtrans for people who want to ask before buying). Followed up same session (`46c2d3c`) with a persistent floating WA button (`src/components/floating-whatsapp.tsx`, bottom-right, WhatsApp green) on every public marketing page — home, `/sistem-akuntansi`, `/kalkulator-hpp`, `/blog` + article pages, `/rekomendasi-alat` — each with a page-specific pre-filled message. Deliberately NOT added to login/signup (kept those forms focused) or the authenticated app/dashboard (internal tool, not a sales touchpoint).

**Google Ads campaign draft (2026-07-18)**: built a full campaign structure (2 campaigns: "Buyer Intent" split into F&B/Retail/Tempat Wisata/Harga ad groups, and "Edukasi" pointing at the 3 blog posts + Kalkulator HPP), published as an Artifact for Melan to reference while setting up the real Google Ads account (I have no login access there). Key competitive intel from research: this niche has well-funded incumbents (Majoo Rp249rb/bln, Moka POS Rp299rb/bln, Pawoon Rp149rb/bln, Qasir free/~Rp700rb-yr) — recommended NOT bidding on expensive head terms ("aplikasi kasir") until there's conversion data, focus budget on vertical/price/educational long-tail instead. Also flagged a critical negative-keyword list ("lowongan kasir", "loker kasir", etc.) since "kasir" is a common job-search term in Indonesian and would otherwise burn budget on job seekers.

**Competitor comparison page shipped same day** (`91e534b`): `/perbandingan` — compares KasirKu's pricing/bundle against Majoo/Moka POS/Pawoon/Qasir, sourced and cited from each competitor's own public pricing pages (not fabricated), directly serves the Google Ads campaign's "Harga & Perbandingan" ad group. Differentiators framed honestly: bundled pricing (no paid add-ons unlike Majoo), Tempat Wisata/Tiket vertical support (competitors don't specifically target this), lifetime-purchase option, transparent up-front pricing. **Competitor pricing WILL go stale over time** — if this page is revisited more than a couple months out, re-verify the 4 competitor prices via web search before trusting the cited figures, since they're a point-in-time snapshot not a live feed.

**Meta Ads**: paused per Melan's request ("kita bahas besok") — he doesn't have a Meta Pixel ID yet. Next session should pick up from: does he have a Pixel ID now, or does he want to be walked through creating a Meta Business Manager + Page + Pixel from scratch (same screenshot-guided pattern as the [[deployment-status]] domain-connection work), or ship the Pixel component code now with the ID to be filled in later. The technical implementation plan (when ready): a `src/lib/analytics/meta-pixel.tsx` component mirroring the existing `google-analytics.tsx` pattern (env-var gated, renders nothing without an ID), wired into `layout.tsx` next to `<GoogleAnalytics />`, plus a `CompleteRegistration` event fired in `src/app/signup/page.tsx` right where `data.session` succeeds (before `router.push("/onboarding")`).

**How to apply**: when Melan brings up marketing/ads next, don't re-explain the CRO findings above from scratch — check whether items 2-3 are still open, then continue instead of re-auditing. If asked "apa lagi yang perlu dibenerin sebelum iklan," this file has the running list.
